import { Contextmenuexample } from './contextmenuexample';

describe('Contextmenuexample', () => {
  it('should create an instance', () => {
    expect(new Contextmenuexample()).toBeTruthy();
  });
});
